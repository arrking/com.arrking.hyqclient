<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/white">

    <include android:id="@+id/layout_title_bar" layout="@layout/tilte_bar"/>

    <RelativeLayout android:id="@id/index_recommend_relativelayout" android:layout_width="fill_parent"
                    android:layout_height="wrap_content" android:layout_below="@id/layout_title_bar">
        <com.handmark.pulltorefresh.library.PullToRefreshListView android:id="@id/recommend_listview"
                                                                  style="@style/info_list"/>
    </RelativeLayout>
    <RelativeLayout android:id="@id/infocat_list_relativeLayout" android:layout_width="fill_parent"
                    android:layout_height="wrap_content" android:layout_below="@id/index_recommend_relativelayout">
        <com.handmark.pulltorefresh.library.PullToRefreshListView android:id="@id/infocat_listview"
                                                                  style="@style/info_list"/>
    </RelativeLayout>

</RelativeLayout>